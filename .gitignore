node_modules/
artifacts/
cache/

# Secrets
.env
.env.*
*.key

# Derived + reward data. Reveal data must never reach a remote before
# startReveal() — publishing it early lets anyone read the prize map.
generated/
reveal-data*.json
*.private.json

# Bulk minting tool and its seed. The repo is public, and publishing this
# would announce that supply is being self-minted — that is a disclosure
# decision, not a default.
scripts/bulk-mint.js
caseflip-minters.json
